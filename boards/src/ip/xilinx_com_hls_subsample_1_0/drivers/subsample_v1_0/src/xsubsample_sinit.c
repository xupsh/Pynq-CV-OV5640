// ==============================================================
// File generated by Vivado(TM) HLS - High-Level Synthesis from C, C++ and SystemC
// Version: 2018.2
// Copyright (C) 1986-2018 Xilinx, Inc. All Rights Reserved.
// 
// ==============================================================

#ifndef __linux__

#include "xstatus.h"
#include "xparameters.h"
#include "xsubsample.h"

extern XSubsample_Config XSubsample_ConfigTable[];

XSubsample_Config *XSubsample_LookupConfig(u16 DeviceId) {
	XSubsample_Config *ConfigPtr = NULL;

	int Index;

	for (Index = 0; Index < XPAR_XSUBSAMPLE_NUM_INSTANCES; Index++) {
		if (XSubsample_ConfigTable[Index].DeviceId == DeviceId) {
			ConfigPtr = &XSubsample_ConfigTable[Index];
			break;
		}
	}

	return ConfigPtr;
}

int XSubsample_Initialize(XSubsample *InstancePtr, u16 DeviceId) {
	XSubsample_Config *ConfigPtr;

	Xil_AssertNonvoid(InstancePtr != NULL);

	ConfigPtr = XSubsample_LookupConfig(DeviceId);
	if (ConfigPtr == NULL) {
		InstancePtr->IsReady = 0;
		return (XST_DEVICE_NOT_FOUND);
	}

	return XSubsample_CfgInitialize(InstancePtr, ConfigPtr);
}

#endif

